[pytest]
# Only tests/ is collected.  docs/archive/test_shopify.py is a leftover
# manual scratch script (it makes a live network request and has no
# assertions), not a real test - it is kept for reference but must not
# run as part of the suite.
testpaths = tests
